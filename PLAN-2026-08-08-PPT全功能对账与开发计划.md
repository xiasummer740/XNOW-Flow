# XNOW 云控 — PPT 全功能对账与开发计划

> 对账对象：`云控讲解.pptx`（98页，10大模块，TikTok 暴力引流系统）
> 对账基准：三层现状 = 后端(FastAPI) / iOS插件(dylib) / 前端(React)
> 日期：2026-08-08 · 版本基线 v1.4.52
> 结论先行：**10 大模块中 4 个已基本闭环，6 个有缺口，4 个完全没做。**

---

## 一、总览表（10 模块 × 三层现状）

| # | 模块 | 后端 | iOS 插件 | 前端 | 总评 |
|---|------|------|---------|------|------|
| 1 | 绑定后台·注册账号 | ✅卡密/配额/激活 | 🟡注册命令卡验证码 | 🟡无批量注册 | 🟡缺：人工激活代绑/切国家/批量注册 |
| 2 | 修改资料 | ✅8类素材库 | 🟡仅昵称+签名 | 🟡无头像/链接 | 🟡缺：头像/链接未打通 |
| 3 | 采集数据截流 | ✅入库/去重/筛选 | ✅5类采集命令 | 🟡页面太裸 | 🟡缺：公共库/直播间进入/AI打标 |
| 4 | 点赞曝光 | 🟡仅透传 | ✅like/batch_like | 🟡无任务化 | 🔴缺：评论点赞任务(核心大项) |
| 5 | 自动发视频 | ✅video_posts | 🔴只能选相册第一项 | ✅有UI | 🔴缺：指定视频选片(核心大项) |
| 6 | 指定视频评论 | 🟡无任务编排 | 🟡发信未确认真实 | 🟡拼凑式 | 🟡缺：任务化+确认发布 |
| 7 | 自动私信 | ✅dm_tasks+话术 | 🟡send_dm脆弱 | 🟡纯文本 | 🔴缺：名片/发视频/直播间/粉丝遍历 |
| 8 | 回关私信 | ❌无 | ❌无 | ❌无 | 🔴完全未做 |
| 9 | 智能翻译 | ❌无 | ❌stub"待实现" | ❌无 | 🔴完全未做 |
| 10 | 快捷指令/批量/养号 | ✅quick_cmd+养号 | ✅养号较完整 | 🟡单号注册 | 🟡缺：批量注册+滑块打码 |

> 🔴=核心缺口（影响主链路/完全没做） 🟡=部分实现有缺口 ✅=基本闭环

---

## 二、PPT 后台截图揭示的"参考产品真相"（vision 分析结论）

从 10 张关键截图读出的信息，决定了几条重要架构判断：

1. **激活是"双路径"**（S6）：自动激活（卡密）+ 人工激活（机器码发给客服→客服后台代绑）。我们只有自动路径，缺"客服代绑"运营闭环。
2. **采集是有"待处理池"的**（S36）：采集→初筛(性别/国家/粉丝数)→批量入库→**AI分析打标(关键词/画像)**→分组。我们缺"筛选入库池"和"AI打标"两步。
3. **公共数据库是杀手锏**（S39）：**1.6亿条**共享用户库，按性别/国家/年龄/关键词筛选，头像带**AI打标(颜值/情绪/表情/眼镜)**。我们完全无"公共库"概念——每个租户数据孤立。**这是商业差异化的核心。**
4. **去重是"分组级操作"**（S38）：按分组执行去重（如"丹麦-男-25-35"组），保留1条/组，有"预览影响"。"已激活用户"组不去重。我们有插入级去重，缺"分组级去重操作"。
5. **评论点赞是"任务池"**（S50）：选账号→选评论分组(2966条)→每号点赞次数(1-1000,推荐≤300防封)→立即提交→任务池执行。我们只有单设备透传。
6. **批量注册依赖"打码平台"**（S96）：接第三方验证码识别平台(冰拓推荐)→全自动滑块。我们 register_account 明确卡在"滑块/验证码无法纯UI自动化"。
7. **批量养号有"行为规则链"**（S98）：自定义行为链(打开APP→搜索→点赞第3条→评论)+实时监控表(账号/状态/运行时长/今日行为数/最近动作/错误日志)。我们养号已有模式/时长/概率，缺行为链配置+监控表。
8. **对标博主采集强调"抖音号(唯一标识)+多账号轮询防封"**（S45）。我们 collect_videos 已能采集，缺"任务化+多号轮询"。

---

## 三、逐项差距与打通逻辑（10 模块）

### 模块 1：绑定后台 · 注册账号

**已有**：卡密激活(licenses.py)、设备自动注册(WS/poll)、配额(quota.py)、账号导入(accounts.py CSV/JSON)、register_account 设备命令(填邮箱/手机+密码)。

| 缺口 | 现状 | 打通逻辑 | 优先级 |
|---|---|---|---|
| 人工激活代绑 | 无"客服代绑"操作 | licenses 已支持生成卡；加"用机器码查设备→绑卡"后台页(admin)：`GET /devices?machine_code=` + `POST /licenses/bind/{card}/device/{udid}` | P2 |
| 切换国家 | 浮窗有58国选择器但无命令无后端无前端 | ①前端/设备选国家→存 device.country ②`switch_country`命令下发 ③设备侧**网络层**切区(依赖代理/加速器App，BH版TikTok检测TCP直连，需确认走系统代理不触发) ④或改TikTok区域设置UI | P2·需架构决策 |
| 批量注册 | 单号 best-effort | 打码平台抽象层 + 设备端滑块识别，见模块10 | P2 |

### 模块 2：修改资料

**已有**：edit_profile 命令(昵称/签名)、materials.py 8类素材(含 avatar/nickname/signature/link)、前端 DeviceControl 只有昵称+签名。

| 缺口 | 打通逻辑 | 优先级 |
|---|---|---|
| 头像修改 | edit_profile 加 `avatar_url` 参数；设备端"点头像→选相册/素材→确认"UI自动化(素材图先下载到相册)；后端素材 avatar 分类已就绪 | P2 |
| 主页链接修改 | edit_profile 加 `link` 参数(现无)；素材 link 分类已就绪 | P2 |
| 自动修改资料(定时批量) | 后端定时任务从 materials 随机取(avatar/nickname/signature/link)→下发 edit_profile 完整参数→设备执行→上报；需先补上头像/链接设备能力 | P1(依赖头像/链接) |

### 模块 3：采集数据截流

**已有**：设备端 collect_fans/videos/comments/live_users/likes；后端 collected_data(插入级去重/筛选 gender/region/followers/分组)；前端 CollectedData 只读列表。

| 缺口 | 打通逻辑 | 优先级 |
|---|---|---|
| 采集池+筛选入库(S36) | collected_data 加 `status=待分析/已入库` 状态机；前端"待处理池"页：筛选(性别/国家/粉丝数/关键词)→勾选→批量入库/删除；采集参数化(数量上限/备注标签，防封) | P1 |
| **公共用户库**(S39) | 新表 `public_users`(跨租户聚合+去重+脱敏，字段=公共池)；collected_data 批量"投喂公共库"；前端公共库筛选页(性别/国家/年龄/关键词)+入库操作 | **P1·商业核心** |
| AI头像打标 | 接 qwen-vl(DASHSCOPE key 已有!)批量分析头像→标签(颜值/情绪/眼镜/年龄)→回写公共库 keyword 字段 | P2 |
| 直播间进入 | 设备端新命令 `open_live`(按主播ID进入直播间)→collect_live_users 才有源头；现状只能采"已进的直播间" | P1 |
| 分组级去重操作(S38) | collected_data 加分组级"执行去重"动作(保留策略:1条/组 or 手机号+保留最新)+前端去重页(分组/未用数/总数/操作)+预览影响 | P2 |

### 模块 4：点赞曝光玩法

**已有**：like/batch_like 设备命令、后端透传、quick_commands 模板。

| 缺口 | 打通逻辑 | 优先级 |
|---|---|---|
| **评论点赞任务**(S50) | ①设备端新命令 `like_comment`(open_video→open评论面板→逐条点赞评论者) ②后端 `comment_like` 任务模型(选账号组+评论分组[来自采集池]+每号次数1-1000 推荐≤300) ③前端任务创建页(执行方式:手动选号/设备编号/选评论分组/次数/立即提交) ④调度→设备队列→进度上报 | **P1·核心大项** |
| 对标博主视频采集(S45) | 后端"采集任务"(指定抖音号唯一标识+数量+指定执行账号多号轮询+清理等待任务)；复用 collect_videos 命令 | P1 |
| 筛选符合数据的视频 | 采集视频入库带筛选(时长/地区/数据)，前端对标库筛选页 | P2 |

### 模块 5：自动发视频 ★核心缺口

**已有**：post_video 命令(**只能选相册第一项，无法指定视频**)、video_posts.py、MediaManagement 素材上传。

**核心缺陷**：`post_video` 注释明确"cannot target specific URL -- picks first album item"。这意味着"导入要发送的视频→自动发"主链路名存实亡。

| 打通逻辑 | 优先级 |
|---|---|
| **方案A(推荐·稳)**: 视频URL→设备下载到相册→相册按文件名/尺寸匹配目标cell→选中→发布。设备端 XNURLProtocol 已有下载能力(截feed URL)，复用做素材下载。 | **P1·核心大项** |
| 方案B(快但糙): post_video 增加 `video_index`(第N个相册项)参数+ `clear_album_after` 用完即删相册，保证"下一个视频固定是刚导入的"。 | P1 备选 |
| 标题文案轮换: 素材库 title 分类已就绪→下发时从分组随机取标题 | P1 |

### 模块 6：指定视频评论

**已有**：comment 命令(开面板填文本发送)、open_video、materials comment 分类、quick_commands。

| 缺口 | 打通逻辑 | 优先级 |
|---|---|---|
| 评论任务化 | 后端"视频评论任务"(video_id + 内容池[素材comment分组] + 设备/账号)→设备 open_video→comment→循环→进度上报；前端任务页(视频ID输入+获取ID引导) | P1 |
| **确认真实发布** | handover 待办："评论流程执行但未确认真实发布"。需真机验证 comment 命令是否真的发出去(发送按钮是否命中、评论出现在评论列表) | **P0 验证项** |

### 模块 7：自动私信 ★核心缺口

**已有**：send_dm(打开用户→Message→填文本→发送，best-effort)、dm_tasks.py、reply_templates.py(关键词自动回复模板，**未接设备聊天**) 、materials dm。

| 缺口 | 打通逻辑 | 优先级 |
|---|---|---|
| 粉丝私信遍历 | 设备端新命令 `dm_fans`(遍历我的粉丝/采集池用户→逐个发信) 或后端编排(open_user→send_dm 循环，从采集池取 target) | P1 |
| 用户名片发送(send_card) | 现有 best-effort 需真机验证+修 share 面板定位；命令参数化 | P2 |
| 发视频给用户 | 设备端 `share_video_to_user`(打开视频→share→选目标用户) 依赖模块5的选片能力 | P2 |
| 直播间分享(share_live) | 依赖 `open_live`(模块3)+ share 面板定位；缺主播ID获取前端引导 | P2 |
| 话术自动回复接通 | reply_templates 目前只是 CRUD，需设备端拦截私信聊天→匹配关键词→自动回复 | P2 |

### 模块 8：回关私信 ★完全未做

**已有**：无任何代码。follow 命令仅单设备透传。

| 打通逻辑 | 优先级 |
|---|---|
| ①设备端 `collect_my_followers`(读"我的粉丝"列表) ②`auto_follow_back`(遍历粉丝列表逐个关注) ③回关后自动私信(带话术)→复用 send_dm ④后端"回关私信任务"(选设备+账号+话术组+数量)→调度→进度上报；前端任务页 | **P1·核心大项** |

### 模块 9：智能翻译 ★完全未做

**已有**：浮窗菜单有"翻译功能待实现"stub，无任何代码。

| 打通逻辑 | 优先级 |
|---|---|
| 后端 `/api/translate` 统一接口(对接 DeepSeek/免费翻译API)；语言方向前端配置 | P1 |
| 聊天翻译: 设备端 XNURLProtocol 拦截私信聊天响应→译文随附展示，或浮窗"翻译模式"监听新消息→上报翻译→浮窗显示 | P2 |
| 注意: 我们的私信是群发工具，聊天翻译价值在"养号回消息"场景 | P3 |

### 模块 10：快捷指令 · 批量注册 · 养号

**已有**：quick_commands.py+前端快捷指令、nurture.py+养号命令(较完整:模式/时长/概率)、register_account best-effort。

| 缺口 | 打通逻辑 | 优先级 |
|---|---|---|
| 批量注册 | ①打码平台抽象层(配置管理页+API对接) ②设备端滑块识别(截图→上传打码→坐标→滑动注入) ③register_account 补验证码步骤 ④批量注册任务(注册池+邮箱/手机池+进度) | P2 |
| 滑块打码 | 打码平台管理页(S96参考: 单账号/状态监控/添加账号)+统一 captcha 服务接口 | P2 |
| 养号行为规则链 | 养号加"行为链配置"(打开→搜索→点赞第N条→评论) + 实时监控表(账号/状态/运行时长/今日行为数/最近动作/错误日志)——现在只有模式/时长/概率 | P2 |
| 快捷指令 | 已实现，PPT 第10章"快捷指令安装"可能指设备端浮窗快捷按钮——确认浮窗已覆盖 | 验证 |

---

## 四、架构级更优建议（超越逐项补齐）

1. **公共用户库 = 商业差异化核心**。参考产品靠 1.6亿公共库卖点。建议优先建 `public_users` 跨租户聚合池（采集数据脱敏投喂→多维度筛选→入库复用），配 AI 头像打标（直接复用现成 DASHSCOPE/qwen-vl key）。这是"我做的东西别人没有"的关键。
2. **统一任务引擎**。当前是"命令透传"。PPT 的"指令创建/设定"本质都是：选账号组+选数据组/素材组+设次数+定时→任务池→调度→设备队列→进度上报。建议建统一 Task 模型（type 枚举: like/comment_like/follow/comment/dm/post_video/collect/follow_back），一次打穿所有模块，避免每功能各写一套调度。
3. **打码平台抽象层**：统一 captcha 服务接口，后续可换多家平台，批量注册/登录风险校验都走它。
4. **风控参数内建**：PPT 全程强调防封（数量上限、推荐值≤300、多号轮询、行为随机化）。把"推荐上限+随机区间"内置到每个任务模型，前端显示推荐值，减少封号。
5. **多账号轮询**：采集/点赞/私信按"账号池"轮询执行（S45 强调），避免单账号高频。后端调度时 round-robin 账号。
6. **素材中心复用**：materials.py 8类素材已是现成内容库，打通到全部"自动XX"任务（改资料/发视频标题/评论/私信话术），一处维护处处生效。

---

## 五、分阶段开发计划

### P0 — 立即（v1.4.52 真机待验证，先闭环）
1. 互动养号崩溃复测（interact/like/follow step 定位）→ 若仍崩继续修
2. 账号资料显示验证（AccountManager /user/ 捕获 + NSUserDefaults）
3. 采集粉丝复测（导航阶段崩溃已修）
4. 采集点赞直播间检测验证（_isInLiveRoom）
5. 备份去重验证（同 aweme_number 更新不新建）
6. **评论发布确认真实性**（模块6 P0验证项）

### P1 — 核心链路补全（直接影响"能不能用"）
1. **模块5 发视频指定选片**（方案A 下载+相册匹配，或方案B index 兜底）
2. **模块4 评论点赞任务**（like_comment 命令 + comment_like 任务 + 前端任务页）
3. **模块8 回关私信**（collect_my_followers + auto_follow_back + 回关私信任务）
4. **模块7 私信链路重做**（粉丝遍历 dm_fans + 任务化）
5. **模块6 视频评论任务化** + 确认发布
6. **模块3 公共用户库**（public_users 表 + 前端筛选/入库页）
7. **模块3 直播间进入 open_live** + 直播间采集任务
8. **模块3 采集池状态机 + 筛选入库前端**
9. 后端 /api/translate（模块9基础）

### P2 — 扩展
1. 模块2 头像/链接修改 + 自动修改资料任务
2. 模块1 人工激活代绑（客服运营闭环）
3. 模块1 切换国家（需架构决策：代理/网络层 vs TikTok UI）
4. 模块10 批量注册 + 滑块打码（打码平台抽象层 + 设备端滑块识别）
5. 模块3 AI头像打标（qwen-vl 批量）+ 公共库关键词
6. 模块3 分组级去重操作（S38）
7. 模块10 养号行为规则链 + 实时监控表

### P3 — 增强
1. 模块9 聊天翻译（拦截聊天→译文展示/自动回复）
2. 模块7 话术自动回复接通设备聊天
3. 模块7 send_card/share_live/share_video_to_user 完善
4. 公共库营销页/多租户共享策略

---

## 六、需要祥哥拍板的 3 个决策

1. **切换国家怎么做**：设备侧走"第三方加速器/代理App"（系统网络层，不改TikTok）还是"改TikTok区域设置UI"？BH版TikTok 检测 TCP 直连会 exit，走系统代理是否触发需实测。建议先做"记录国家属性+前端选择"占位，网络切换独立验证。
2. **公共用户库的范围**：跨租户共享（所有用户采集数据聚合，脱敏）是否接受？涉及数据合规，建议仅"去标识化"字段进公共库。
3. **批量注册投入**：批量注册依赖打码平台（付费）+ 设备端滑块识别，工作量不小且成功率依赖打码服务。是否本轮做，还是先保"账号导入+养号+互动"主线？

---

## 七、三个决策的方案展开

### 决策 1：切换国家怎么做

**技术事实**
- TikTok 账号的"国家"由**注册时的网络出口 IP** 决定；改 App 内语言/地区设置**不改变账号国家**。所以"切换国家"= 换网络出口。
- 已确认约束：BH 版 TikTok 按**目标 IP** 检测到 VPS 的连接（`bh-tiktoken-network-detection`），我们靠 NSURLProtocol piggyback 蹭 TikTok 自己的网络会话通信（`v1.3-piggyback-success`，反馈回路 100% 工作）。

**方案 A：系统代理/加速器 + 插件校验引导（推荐）**
- 用户在手机装"加速器/机场"App，选目标国家 → 全局代理 → TikTok 流量从该国出口出。
- 插件侧：piggyback 请求带设备标识，**后端用请求出口 IP 做 GeoIP**，识别设备"当前国家"，前端/浮窗显示。
- 注册流程：前端设目标国家 → 设备检测当前国家 → 不匹配则提示"去加速器切到 XX"→ 匹配才允许注册。
- ✅ 稳、不改网络层、复用现成 piggyback；符合 PPT S9"切换国家（视频教程）"（视频很可能就是教装加速器）。
- ⚠️ 依赖用户装代理 App；**需真机实测** BH 检测对"代理出口 IP 的 piggyback 请求"是否豁免（代理不改目标 IP，理论上不受影响）。
- 工作量：后端 GeoIP 国家识别 + `device.country` 字段 + 前端国家选择 + 注册前置校验 ≈ **0.5-1 天**。

**方案 B：插件内置 VPN** —— iOS 用户态 dylib 建不了系统级 VPN（需网络扩展/越狱），成本极高，**放弃**。
**方案 C：改 TikTok 区域 UI** —— 不改变账号国家属性，**不可靠，放弃**。

### 决策 2：公共用户库做多大

**背景**：PPT S39 显示参考产品有 **1.6 亿条公共库**，按性别/国家/年龄/关键词筛选，头像带 AI 打标（颜值/情绪/表情/眼镜）。这是我们完全没有的商业差异化点。

**完整版（推荐）：公共池 + AI 头像打标**
- 新表 `public_users`：只存**去标识化公开资料**（头像URL/昵称/抖音号/性别/国家/粉丝数/作品数/签名/**关键词标签**；剔除 密码/token/邮箱/手机/设备）。
- 投喂：各租户采集数据中质量达标的 → 批量投喂，按 `aweme_id` 全局去重。
- 筛选：跨租户按 性别/国家/年龄/粉丝区间/关键词 筛 → 批量"复制入库"到自己分组。
- AI 打标：接 **qwen-vl（DASHSCOPE key 现成）** 批量分析头像 → 颜值/情绪/年龄/眼镜/性别 → 写 keyword → 还原 PPT"颜值:XX.XX"效果。
- 存储：当前量级 SQLite 够，预留 PostgreSQL 迁移路径。
- 工作量：表+投喂接口+筛选前端 ≈ **1-1.5 天**；AI 打标批处理 +**1-1.5 天**。

**轻量版：只做公共池+筛选+投喂**（1-1.5 天，不做 AI 打标）。
**暂不做**：保持各租户数据孤立。

### 决策 3：批量注册投入

**背景**：PPT S96 显示参考产品接**打码平台**（推荐"冰拓"）做全自动滑块，批量注册依赖它。我们 `register_account` 明确卡在"滑块/验证码无法纯 UI 自动化"。

**完整方案**：后端 `captcha.py` 打码平台抽象层（配置账号/API key，统一 `POST /api/captcha/solve` 滑块截图→坐标）→ 设备端遇滑块截图上传→坐标注入滑动→批量注册任务（账号池/邮箱手机池/进度）。工作量 **2-3 天**。
- 💰 成本：打码平台按次计费（约 1-5 分/次），识别率决定成功率。
- ⚠️ **风险**：TikTok 注册风控强，即使过滑块也可能要**手机短信验证码**（需再接"接码平台"，又一笔成本与复杂度）。

**务实路线（推荐）：本轮暂缓批量注册**，维持"人工注册/买号 + CSV 导入"（accounts.py 已支持），把人力投到 P1 核心链路（发视频选片 / 评论点赞任务 / 回关私信）。批量注册作为 P2 增值项，等主线闭环后小规模试点验证成功率再投入。

---

## 八、拍板结论与执行顺序（2026-08-08 祥哥确认）

### 拍板结果
| 决策 | 结论 |
|---|---|
| 切换国家 | **方案 A**：加速器 + 插件 GeoIP 校验引导 |
| 公共用户库 | **完整版**：公共池 + AI 头像打标（qwen-vl） |
| 批量注册 | **暂缓**，投 P1 主线（维持 CSV 导入） |

### 本轮执行顺序（按依赖排序，尽量先做不依赖真机的）

**第一批 · 后端+前端先行（无需装 IPA，马上可验证）**
1. 公共用户库完整版：`public_users` 表 + 去标识化投喂 + 跨租户筛选/入库 + AI 头像打标批处理（qwen-vl）
2. 统一任务引擎基础模型（type 枚举 + 账号组/数据组/素材组 + 次数/随机区间/风控上限）→ **顺带提供"任务状态查询接口"**（供手机端运行监控卡片订阅）
3. 切换国家打通的后端部分：`device.country` 字段 + GeoIP 识别当前出口国家 + 前端国家选择（加速器引导文案）
4. 后端 `/api/translate` 统一翻译接口（对接 DeepSeek/免费翻译，为手机端翻译打底）

**第二批 · 设备端命令 + 浮窗补丁（改 dylib → 构建 → 装机，攒批一次发）**
5. 发视频指定选片（方案A：视频URL下载到相册 + 按文件名/尺寸匹配目标cell）
6. `like_comment`（打开视频→评论面板→逐条点赞评论者）
7. `open_live`（按主播ID进入直播间）+ 直播间采集
8. `collect_my_followers` + `auto_follow_back`（回关私信基础）
9. `dm_fans`（遍历粉丝私信）
10. **浮窗补丁 A**（手机端欠缺项，同批装机；依赖第一批接口）：
    - 激活视图加「复制机器码」按钮（人工激活路径顺手补全）
    - 切换国家打通：选国家 → 上报 `device.country` → 浮窗显示"当前XX 目标XX" → 引导去加速器
    - **运行监控卡片**：订阅后端任务状态接口 → 浮窗显示"运行中任务/进度/最近日志/停止"
    - 快捷指令管理页：拉后端 `quick_commands` → 一键执行
    - 养号页加实时行（今日行为数/最近动作/错误）
    - 绑定成功页显示 APIID

**第三批 · 任务编排 + 前端（打通 PPT"指令创建"页）**
11. 评论点赞任务（选账号组+评论分组+次数≤300）
12. 回关私信任务（粉丝池→回关→话术私信）
13. 视频评论任务 + 发视频任务（标题轮换）+ 采集任务（对标博主/多号轮询）

**第四批 · P2 扩展**
14. 头像/链接修改 + 自动修改资料任务
15. 人工激活代绑（客服运营闭环，后台页）
16. 分组级去重操作 + 养号行为规则链 + 实时监控表（后台）
17. 打码平台抽象层 + 批量注册入口（含手机端注册页）
18. 手机端聊天翻译（拦截聊天 → 译文浮层，依赖第一批 `/api/translate`）

> 📱 手机端欠缺项已全部并入：第 10 项浮窗补丁 A 覆盖 6 项（复制机器码/切国家/运行监控/快捷指令页/养号实时行/APIID），第 17/18 项覆盖批量注册入口与翻译。

> ⚠️ P0 真机验证项（互动养号崩溃/账号资料/采集粉丝/备份去重/评论发布真实性）依赖祥哥设备，**与第一批并行推进**——装 v1.4.52 后测，结果回填到本文档。

---

## 九、手机端功能对账（PPT 手机页面 vs iOS 插件浮窗）

> 手机端 = iPhone 上插件的浮窗界面 + TikTok 原生界面上叠加的运行状态。
> vision 确认：PPT 的翻译/聊天/注册截图是 **TikTok 原生界面**（收件箱/聊天/未登录引导页），插件负责引导与叠加。

### 9.1 手机端功能树（PPT 完整）

**A. 激活与绑定**（S5-8）
1. 激活界面·双路径：自动激活（输入卡密）/ 人工激活（复制机器码→发客服→客服后台代绑）
2. 绑定云控后台（设备编号 + APIID + 确定）
3. 查看 API ID / 验证绑定成功

**B. 切换国家**（S9-10）
1. 大洲分组国家选择器（亚洲/欧洲/美洲/非洲，已配置节点即时切换、无确认按钮）

**C. 账号管理**（S11-15）
1. 注册账号（Sign up 引导 → 邮箱/手机注册）
2. 切换账号（快捷操作中枢 + 账号轮换）
3. 账号信息显示（序号.国旗 昵称 + 资料）

**D. 自动运行监控**（S42/44/66）
1. 自动关注运行中：实时日志 + 粉丝数增长监控 + 手动干预
2. 自动评论区点赞（运行监控）
3. 自动私信（运行监控）

**E. 智能翻译**（S88-89）
1. 翻译语言选择
2. 聊天内容自动翻译（TikTok 原生界面长按翻译 / 自动识别+译文浮层）

**F. 快捷指令安装**（S91-94）
1. 快捷指令设定（iOS 快捷指令/插件快捷按钮）

**G. 批量注册与养号**（S95-98）
1. 批量注册（打码平台滑块）
2. 批量养号：参数配置（随机+区间）+ 行为规则链 + 实时监控表（账号/状态/运行时长/今日行为数/最近动作/错误日志）

### 9.2 我们的浮窗现状（XNFloatingPanel.m 全貌）

| PPT 手机功能 | 我们现状 | 判定 |
|---|---|---|
| A1 激活·自动(卡密) | ✅ 激活视图：UUID卡片+卡密输入+确定 | ✅ |
| A1 激活·人工(客服代绑) | 🟡 激活视图有"联系客服+机器码"提示文案，但**无"复制机器码"按钮**（主菜单才有） | 🟡 小缺 |
| A2 绑定后台 | ✅ 绑定视图：设备编号(1-20)+APIID+确定 | ✅ |
| A3 查看APIID | 🟡 手机端无查看APIID入口（需后台） | 🟡 小缺 |
| B 切换国家 | 🟡 浮窗有**58国平铺列表**，仅本地存储，**无大洲分组/无网络切换/无后端同步**（决策1已定A方案） | 🟡 待打通 |
| C1 注册账号 | 🟡 无手机端注册入口；仅后端命令 register_account（best-effort，卡验证码） | 🟡 |
| C2 切换账号 | ✅ 账号管理子菜单：账号列表+切换确认 | ✅ |
| C3 账号信息 | ✅ 序号.国旗 昵称 + 粉丝/关注/国家/TK 详情 | ✅ |
| D1 自动关注运行监控 | 🟡 无手机端实时运行页；batch_follow 后台触发，养号有日志浮窗 | 🟡 |
| D2 评论点赞运行监控 | ❌ 无 like_comment 命令（模块4大缺口） | 🔴 |
| D3 自动私信运行监控 | 🟡 send_dm 命令有，无运行监控页 | 🟡 |
| E 智能翻译 | ❌ 浮窗翻译菜单 stub "翻译功能待实现" | 🔴 |
| F 快捷指令安装 | 🟡 主菜单本身就是 13 项快捷指令集，但无"指令配置/安装"管理页 | 🟡 |
| G1 批量注册入口 | 🟡 无手机端入口（后端命令 best-effort） | 🟡 |
| G2 批量养号 | ✅ 养号子菜单（互动/纯浏览/设置时长/停止）+ 日志；🟡无实时监控表/行为规则链 | 🟡 |

### 9.3 手机端欠缺清单与打通逻辑

| # | 欠缺 | 打通逻辑 | 优先级 |
|---|---|---|---|
| 1 | 激活视图缺"复制机器码"按钮 | 激活视图加复制按钮（机器码已有，复用主菜单逻辑） | P1·改浮窗顺手做 |
| 2 | 切换国家未打通 | 浮窗国家选择 → 上报 `device.country` → 后端 GeoIP 识别当前国家 → 浮窗显示"当前XX，目标XX"引导去加速器切区（决策1 A方案） | P1 |
| 3 | 运行监控页缺失 | 后端任务进度已有 step 遥测基础 → 浮窗加"运行中任务"卡片（当前任务/进度/日志/停止按钮），订阅后端任务状态 | P1 |
| 4 | 养号实时监控表 | 浮窗养号页加"本轮行为数/最近动作/错误"实时行（复用 step 遥测），对齐 S98 | P2 |
| 5 | 手机端注册/批量注册入口 | 补 register_account 命令 + 浮窗注册页（依赖打码平台，暂缓） | P2·随批量注册 |
| 6 | 快捷指令管理页 | 浮窗加"指令库"子页（从后端 quick_commands 拉取，一键执行） | P2 |
| 7 | 翻译 | 后端 /api/translate + 浮窗翻译模式（拦截聊天→译文） | P2/P3 |
| 8 | 手机端 APIID 查看 | 绑定成功页显示 APIID（已有存 NSUserDefaults） | P3 |

> 手机端结论：**激活/绑定/账号管理/养号四大块已具备**；缺口集中在 **切换国家打通、运行监控可视化、翻译、评论点赞命令**。多数打通依赖后端任务化 + 设备命令补齐，浮窗改动不大。

---

## 十、第一批第1项「公共用户库完整版」验收报告（2026-08-08 23:00 前后）

**已交付并部署到 VPS（192.129.210.52）**，后端 + 前端均已上线。

### 新增/改动
| 文件 | 说明 |
|---|---|
| backend/models/public_user.py | PublicUser 模型（13列，仅公开资料，aweme_id全局去重，ai_tagged标记） |
| backend/schemas/public_user.py | Pydantic schema |
| backend/routers/public_users.py | 5个端点：list/stats/feed/copy/tag |
| backend/config.py | 新增 DASHSCOPE_API_KEY/MODEL/BASE_URL |
| backend/database.py | _migrate_public_users() 幂等建表 |
| backend/main.py | 挂载模型+路由 |
| login/src/pages/PublicLibrary.tsx | 前端公共用户库页 |
| login/src/Dashboard.tsx | 内容组加"公共用户库"菜单 |

### 验证结果（VPS 实测，全部通过）
1. ✅ 建表：public_users 13列齐全（PRAGMA 确认）
2. ✅ 投喂 feed：有头像2条入库、无头像过滤、无ID优雅跳过、重复跳过
3. ✅ 筛选 list：gender/country/min_followers 多维正确
4. ✅ 统计 stats：总数/已打标/性别/国家分布正确
5. ✅ 复制 copy：跨租户用户 INSERT 成功入库（分组/粉丝/aweme正确）；同租户重复自动跳过（去重生效）
6. ✅ AI 打标 tag：**真实 qwen-vl 调用 2.6s**，返回结构化标签写入 keyword（颜值/情绪/年龄/眼镜/性别/风格），ai_tagged=1
7. ✅ 打标守卫：未配置 key 时返回 400 清晰提示，不烧记录；失败不标记可重试
8. ✅ 前端：tsc+vite 构建通过，部署后 bundle 含公共用户库页面标记，线上 index 引用正确

### 运维备注
- VPS `/opt/xnow-flow/.env` 已配置 DASHSCOPE_API_KEY（sk-ws-...，长度117，已验证有效）
- 生产 DB 已清理全部测试数据（public_users 0 条待真实采集投喂）
- 前端已上线，后台左侧菜单"内容"组 →「公共用户库」入口

### 待后续
- 真实采集数据到位后：前端一键投喂 → AI 打标 → 筛选 → 复制到分组 全流程可用
