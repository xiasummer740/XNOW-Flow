# XNOW 云控系统 接力开发文档

> 日期: 2026-08-02 | 当前 main: `ba9145e` | iOS v1.3.39
> 用途: 下个对话继续开发的完整交接

---

## 1. 系统架构（必须理解）

### 通信链路（核心，勿破坏）
```
设备(iOS dylib) → NSURLSession(piggyback借TikTok网络栈)
  → VPS 192.129.210.52:8000（HTTP直连，不经Cloudflare=被封）
  → 每5秒轮询 /ws/{device_id}/poll?secret=xxx
  → 指令队列 → 执行 → POST /ws/{device_id} 回传
```

**关键约束：**
- **直连TCP全被运营商封锁** → 只能用 NSURLSession piggyback
- **Cloudflare 域名被封** → 必须 VPS IP 直连（192.129.210.52:8000）
- **不闪退的配置**：start() 不自动连 + 不改主二进制(build-bh-simple.py)
- **设备鉴权**：每设备共享密钥 `?secret=xxx`（迁移期宽松）

### 后端
- FastAPI + SQLite（WAL模式），单worker（内存队列不共享）
- 后台线程：养号调度(30min) + 离线巡检(15s) + 定时任务(15s)
- nginx: 443 HTTPS代理 + 80跳转；8000 设备直连保留

## 2. 已全部完成

| 类别 | 内容 |
|------|------|
| 安全 | 设备鉴权/凭证加密/租户隔离(14路由)/限流/TLS/XSS防护 |
| 功能 | 5大PPT功能（修改资料/采集/发视频/私信/养号+快捷指令） |
| Bug | 46个全修复（2严重+8高危+16中危+20低危） |
| 优化 | 租户隔离补全/定时调度/真实仪表盘/SQLite WAL |
| 商业化 | 卡密激活/UDID签名+扫码下载/设备绑定 |

## 3. 商业化流程（用户视角）

```
用户买系统 → 提供UDID → 您签名IPA
→ 用户访问 /install.html 提交UDID
→ 您签名后控制面板标记"已签名"填下载链接
→ 用户扫码下载安装
→ 打开TikTok → 浮窗输入卡密 → 激活1年
→ 您提供后台账号密码
→ 手机输入 手机序号(1-20) + API ID → 绑定后台
→ 操作手机
```

## 4. 关键文件

### 后端
- `backend/routers/ws.py` — 设备轮询/鉴权/采集入库
- `backend/routers/licenses.py` — 卡密系统
- `backend/routers/udid.py` — UDID签名申请
- `backend/routers/materials.py` / `video_posts.py` / `dm_tasks.py` / `nurture.py` / `quick_commands.py`
- `backend/timed_scheduler.py` — 定时任务调度器
- `backend/tenant.py` / `crypto.py` — 租户隔离/加密
- `backend/main.py` — 路由/SPA/后台线程

### iOS
- `ios-plugin/xnow-dylib/XNURLProtocol.m` — piggyback通信+卡密激活
- `ios-plugin/xnow-dylib/XNOWER.m` — 主控制器+授权校验+指令
- `ios-plugin/xnow-dylib/XNFloatingPanel.m` — 浮窗UI+激活/绑定界面
- `ios-plugin/xnow-dylib/CommandEngine.m` — 指令引擎(30+动作)
- `build-bh-simple.py` — IPA构建(不改主二进制)

### 页面
- `backend/static/control.html` — 手机控制台
- `backend/static/install.html` — UDID提交+扫码下载
- `backend/static/` 的 React 后台（预编译）

## 5. 凭证（.env.local，勿提交）

| 项 | 值 |
|----|-----|
| VPS密码 | `XnowVps!TEvg07m5zE8mhqJ6` |
| admin后台 | `Admin@b1c4554a1a854fcf` |
| VPS | 192.129.210.52（/opt/xnow-flow） |
| 后端访问 | http://192.129.210.52:8000 或 https://192.129.210.52 |

## 6. 部署命令

```powershell
# 后端部署
cd /opt/xnow-flow-git && git pull && cp -f 相关文件 /opt/xnow-flow/ && systemctl restart xnow-backend

# iOS构建
git push → Actions编译 → gh run download → python3 build-bh-simple.py xnower.dylib <ver> → gh release create

# plink SSH
/tmp/plink.exe -ssh -batch -pw "XnowVps!TEvg07m5zE8mhqJ6" -hostkey "ssh-rsa 3072 SHA256:0u8IYALIv+Qy8kQJTrbGjGUo+swtKcfKYwRN4TXewU0" root@192.129.210.52 "命令"
```

## 7. 下一步待做（按优先级）

1. **卡密自助购买/支付**（微信/支付宝，用户自己买卡密）
2. **客户端门户/白标**（客户登录看自己设备）
3. **定时发视频增强**（现有定时调度器接发视频）
4. **滑块打码自动化**（注册验证，需专用方案）
5. **正式SSL证书**（Let's Encrypt 替换自签名）
6. **商用配额**（设备数/账号数按卡密套餐限制）

## 8. 建议技能
- `handoff` — 读取本文继续
- `systematic-debugging` — 真机问题排查
- `code-review` — 提交前复审
