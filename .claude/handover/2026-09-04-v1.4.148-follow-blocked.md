# 2026-09-04 follow 三层反自动化墙证伪 + 拍板搁置

## 一句话
follow 修到 v1.4.148（方向 D：深链进 profile 点 UIControl）装机验证**证伪**——不是实现 bug，是 TikTok 43.7.0 三层反自动化墙。祥哥拍板**搁置 follow，下批对已装 148 跑全量验证清单**。

## 做了什么
1. 对祥哥装的 148 后台/远端验证 follow → `after: '<profile未到达>'`（隔离 + 回归复验一致）
2. 隔离测试证伪方向 D 前提（进作者 profile 的三条路全断）：
   - 深链 `snssdk1233://user/<username>` 不导航
   - 深链 `snssdk1233://user/<数字uid>` 不导航
   - 点头像（open_profile 空 username）→ touch_diag 证实 tap 命中 `AWEStoryAvatarButton`（is_control=True + target_action `onUserAvatarViewClicked:` 已注册）→ **仍不导航**
3. 根因定位到 HID：ctx_probe `ctx=0, servicesCount=1` → **App Store 沙盒拿不到 digitizer ContextID** → `_hidTapAtPoint:` 永远 NO → 全回退 KVC 假触摸（TikTok 手势层必拒）
4. 查证 net_like（方向 C 网络层）**从未成功过**（server.log 历史 9 次全败：feed SwiftNIO 抓不到 + X-Gorgon 签名未解）
5. 回归六命令 ground truth：open_tab/go_home/open_search/backup ✅（全走直调 API 不靠触摸）；like ✅（⚠️可能污染：没先取消赞）；follow ❌
6. ISSUES.md 记录决策 + 下批清单（A 账号/B 面板/C 触摸墙 16 项），提交推送（b3f13e9）

## 关键认知（以后别再走弯路）
- **43.7.0 能自动化的是「直调 UIKit 运行时」（setSelectedIndex 切 tab / 直调 VC），不是触摸**
- 触摸层（feed 右侧互动控件）Swift 手势验真实触摸，sendActions/gr_fire/KVC 全拒；HID 是唯一真触摸但沙盒拿不到 ContextID → 触摸类功能在 43.7.0 App Store 装法下**不可自动化**
- 深链全部 scheme 被忽略；网络层缺签名
- follow 剩唯一自动化路径 = 方向 E（直调 VC 构造 profile），未实施，不确定性高，待祥哥特批预算才投

## 决策副作用
- 搁置 follow = 已积累的 follow 用户故事/预期无限期延后，产品侧需知晓自动关注不可用
- like 的「真成功」证据存疑（回归未先取消赞），下次验证 like 应**先取消赞再点**（但这又依赖触摸墙……like 到底真不真过，值得单测一次）

## 下一步
- 对已装 148 跑 ISSUES「下批装机验证清单」16 项（任务 #37），逐项记三态，验过翻「已验证」
- 重点 C 组会撞触摸墙（保存视频/改昵称/search），提前预期可能失败
- 相关文件：ISSUES.md 148 段、`.claude/handover/INDEX.md` 需加本条目
