# v1.4.88 全自动真机验证（2026-08-15）

> 完整证据：`VERIFY-REPORT-2026-08-15.md`（根目录）+ ISSUES.md 状态更新
> 验证方式：后端 API → 设备 WS 命令通道，全程远程，祥哥零操作。证据=server.log(polled/result/touch_diag/state_diag) + DB

## 验证结论
- ✅ 通：命令通道、点赞(真成功 state_diag Video liked)、open_search、scroll_down、截图/ui_scan、页面检测强化(feed直播卡片不再误判live)、首页菜单重构(无采集三件套)、无崩溃、激活(license 200)
- ⚠️ 代码/配置正确但真机不可达：我的/别人主页区分、各页菜单（被 P0-2 导航坏阻塞）
- ❌ collected_data=0 / accounts=0

## 4 个确认 bug（修完这4个采集/关注/评论类全解锁）
1. **🔴 search_keyword 卡死拖垮设备**：`_performSearchKeyword` 无超时 + `_execQueue` 串行无命令级超时 → 一条命令卡死阻塞通道+心跳 → WS 掉线标记离线（本次 09:15:59 掉线实锤）。修复：命令加 dispatch_after 兜底。
2. **🔴 远程导航全失效**：`open_tab` 注入 gesture 假成功(isSelected=False, TTKTabBarButton target_actions=[])切不了tab；`open_profile` 深链无跳转 + 头像类漂移(AWEStoryAvatarButton 无 acc_id=avatar, 旧类 AWEPlayInteractionUserAvatarView 已消失) + 固定坐标兜底点错(0.08w,0.82h=左下 vs 头像右上)。修复：open_profile 用可见性+位置+多候选类找头像；open_tab 改真实触摸投递。
3. **🟠 评论按钮按屏幕外**：`_openCommentPanel` 用 `_findViewWithAccessibilityIdentifier:`（不筛可见性）命中 y=1170 预取按钮点 AWEMaskWindow。对照：like 用 `_findVisibleViewWithAccId` 真成功。修复：评论类查找全改可见性过滤。
4. **🟠 任务状态全误报**：task_engine.py:116-124 对无 target 远程指令直接判 failed（连成功执行的都显示"失败/无有效目标单元"）。修复：远程指令按设备返回结果标记。

## 踩坑/经验
- DB 时间=UTC，server.log=BST，差 +1h，对照证据时注意换算
- tasks 表列名是 name/device/target（无 device_id 列）
- 设备离线后不会自动重连（poll 无重连 bug 仍在）→ 手机需重新打开 TikTok 才能复连
- build-bh-simple.py 不传版本号默认 "dev" → 下次构建传 `v1.4.89`
- `.env.local` source 时会有 `line 4: 3072: command not found` 噪音，无害（第4行是被 shell 当命令执行的残留，见下）
- **`.env.local` 第4行疑似有裸 token**（source 时报 "3072: command not found"），下次排查是否把密码写进了 profile —— 但**绝不打印该文件内容**

## 待办
- 祥哥拍板是否授权修 P0/P1 四bug → 修完重装 IPA → 全自动复验采集/关注/评论
